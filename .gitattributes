# Normalize text files to LF in the repo (Mac/Linux CI + Windows dev).
* text=auto eol=lf

# Windows scripts keep CRLF when present.
*.{cmd,[Cc][Mm][Dd]} text eol=crlf
*.{bat,[Bb][Aa][T]} text eol=crlf
