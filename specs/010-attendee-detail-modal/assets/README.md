# Design reference assets — Attendee Detail Modal

Two reference screenshots were shared during the `/grill-with-docs` session for this feature (2026-07-17) but could not be extracted directly from the chat — they need to be saved here manually.

**Pending — drop these files into this folder:**

| Expected filename | Content |
| :--- | :--- |
| `attendee-detail-modal-default.png` | Default (collapsed) state — Basic Information card + Attendee Journey timeline scoped to the current Event, "Show all communications" link visible but not expanded. Subject shown: "Amara Okafor". |
| `attendee-detail-modal-expanded.png` | Expanded state — same modal after clicking "Show all communications": toggle now reads "Show event lifecycle only"; timeline includes non-Event items ("Post-Event Thank You — Executive Roundtable sent", "Developer Newsletter — Q4 Digest sent") each tagged "OTHER CAMPAIGN" (tag wording superseded in the shipped design — see `CONTEXT.md` § Attendee communications view; use "OTHER DISPATCH" or the named other-Event tag instead). |

Once added, reference them from `spec.md` / `plan.md` as `assets/attendee-detail-modal-default.png` and `assets/attendee-detail-modal-expanded.png`.
